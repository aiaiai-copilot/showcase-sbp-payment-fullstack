{
  "name": "showcase-sbp-payment-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "YooKassa Payment Integration Demo - Full Stack Monorepo",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "test:backend": "npm test --prefix backend",
    "test:frontend": "npm test --prefix frontend",
    "test:contract": "npm test --prefix backend -- -t Contract && npm test --prefix frontend -- -t Contract",
    "build": "npm run build --prefix frontend",
    "type-check": "npm run type-check --prefix backend && npm run type-check --prefix frontend",
    "clean": "rm -rf backend/node_modules backend/dist frontend/node_modules frontend/dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aiaiai-copilot/showcase-sbp-payment-fullstack.git"
  },
  "keywords": [
    "yookassa",
    "payment",
    "sbp",
    "react",
    "fastify",
    "typescript",
    "monorepo"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
